# Secrets — never commit
.env

# Cached audio, lyrics, models
backend/cache/
__pycache__/
*.pyc

# Built Italian dictionary + the large raw Wiktionary dump (rebuild locally)
backend/data/*.sqlite
backend/data/*.jsonl
